fun main(args: Array<String>) {
    var count = 11//we set some initial variable
//works if condition is true
    while(count<=12){
        if (count > 4){
        println("It Looping $count")}
        count  += 1// or count++ means add 1 to
//increment/decrement count so as one ...time it will be 11 and make condition false
    }
}